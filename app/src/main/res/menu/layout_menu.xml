<?xml version="1.0" encoding="utf-8"?>

<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/staggeredGridView"
        android:icon="@drawable/staggered_menu_button_image"
        android:title="@string/staggeredGridView"
        app:showAsAction="always"/>

    <item
        android:id="@+id/gridView"
        android:icon="@drawable/grid_menu_button_image"
        android:title="@string/gridView"
        app:showAsAction="always" />

    <item
        android:id="@+id/linearViewVertical"
        android:icon="@drawable/list_menu_button_image"
        android:title="@string/linearViewVertical"
        app:showAsAction="always" />
</menu>